services:
  - type: web
    name: riffraff-stems
    env: python
    rootDir: backend-stems
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port 10000
    plan: free
